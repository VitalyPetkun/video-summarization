name: dev

on:
  push:
    branches: [main]

jobs:
  Build:
    runs-on: self-hosted

    steps:
      - name: all
        run: |
          cd /home/server/reps/video-summarization
          git reset --hard origin/main
          git pull
          npm install
          npm run build
          sudo bash kill
          sudo bash start
    
